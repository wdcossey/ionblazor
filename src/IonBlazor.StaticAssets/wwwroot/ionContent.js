export function getScrollElement(element) {
    return null;//element.getScrollElement();
}

export function scrollByPoint(element, x, y, duration) {
    element.scrollByPoint(x, y, duration);
}

export function scrollToBottom(element, duration) {
    element.scrollToBottom(duration);
}

export function scrollToPoint(element, x, y, duration) {
    element.scrollToPoint(x, y, duration);
}

export function scrollToTop(element, duration) {
    element.scrollToTop(duration);
}