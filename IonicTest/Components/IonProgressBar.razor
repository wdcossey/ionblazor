@inherits IonControl

<ion-progress-bar @attributes="@Attributes"
                  buffer="@Buffer"
                  class="@Class"
                  color="@Color"
                  mode="@Mode"
                  reversed="@Reversed"
                  type="@Type"
                  value="@Value">
    
</ion-progress-bar>

