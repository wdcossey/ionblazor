@inherits IonComponent

<ion-refresher @ref="@_self"
               @attributes="@Attributes"
               class="@Class"
               close-duration="@CloseDuration"
               disabled="@Disabled"
               pull-factor="@PullFactor?.ToString("#.#")"
               pull-max="@PullMax"
               pull-min="@PullMin"
               snapback-duration="@SnapbackDuration">
    @ChildContent
</ion-refresher>