export function getSelected(element) {
    return element.getSelected();
}

export function getTab(element, tab) {
    return element.getTab(tab);
}

export function select(element, tab) {
    return element.select(tab);
}