@namespace IonBlazor.Components
@inherits IonJsContentComponent

<ion-refresher @ref="@IonElement"
               @attributes="@Attributes"
               close-duration="@CloseDuration"
               disabled="@Disabled.AsString()"
               pull-factor="@PullFactor?.ToString("#.#")"
               pull-max="@PullMax?.ToString()"
               pull-min="@PullMin?.ToString()"
               snapback-duration="@SnapbackDuration">
    @ChildContent
</ion-refresher>