
@typeparam TItem where TItem: notnull
@inherits IonList

<ion-list @ref="@_self"
          @attributes="@Attributes"
          inset="@Inset"
          lines="@Lines"
          mode="@Mode">
    @foreach (var item in ItemsSource)
    {
        @ItemTemplate(item)
    }
</ion-list>