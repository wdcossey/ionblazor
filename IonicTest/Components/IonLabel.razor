@inherits IonControl

<ion-label @attributes="@Attributes"
           class="@Class"
           color="@Color"
           mode="@Mode?.ToString().ToLowerInvariant()"
           position="@Position">
    @ChildContent
</ion-label>