@inherits IonComponent

<ion-tab-button @attributes="@Attributes"
                class="@Class"
                disabled="@Disabled"
                download="@Download"
                href="@Href"
                layout="@Layout"
                mode="@Mode"
                rel="@Rel"
                selected="@Selected"
                tab="@Tab"
                target="@Target">
    @ChildContent
</ion-tab-button>
