@namespace IonBlazor.Components
@inherits IonContentComponent

<ion-item-option @ref="_self"
                 @attributes="@Attributes"
                 class="@Class"
                 color="@Color"
                 disabled="@Disabled"
                 download="@Download"
                 expandable="@Expandable"
                 href="@Href"
                 mode="@Mode"
                 rel="@Rel"
                 target="@Target"
                 type="@Type">
    @ChildContent
</ion-item-option>